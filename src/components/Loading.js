import React from 'react'

/**
* @author
* @function Loading
**/

const Loading = (props) => {
  return(
    <div>
    <h1 className="loadingDiv">Loading...</h1>
    </div>
   )

 }

export default Loading